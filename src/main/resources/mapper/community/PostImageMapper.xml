<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ditto.community.repository.PostImageMapper">

    <select id="findByPostId"
            resultType="com.ditto.community.repository.PostImageMapper$PostImageRow">
        SELECT post_image_id,
               post_id,
               object_key,
               sort_order
          FROM post_image
         WHERE post_id = #{postId,jdbcType=NUMERIC}
         ORDER BY sort_order ASC,
                  post_image_id ASC
    </select>

    <select id="findByPostIdAndIds"
            resultType="com.ditto.community.repository.PostImageMapper$PostImageRow">
        SELECT post_image_id,
               post_id,
               object_key,
               sort_order
          FROM post_image
         WHERE post_id = #{postId,jdbcType=NUMERIC}
           AND post_image_id IN
        <foreach collection="imageIds" item="imageId" open="(" separator="," close=")">
               #{imageId,jdbcType=NUMERIC}
        </foreach>
         ORDER BY sort_order ASC,
                  post_image_id ASC
    </select>

    <select id="findMaxSortOrder" resultType="int">
        SELECT COALESCE(MAX(sort_order), -1)
          FROM post_image
         WHERE post_id = #{postId,jdbcType=NUMERIC}
    </select>

    <insert id="insert"
            parameterType="com.ditto.community.repository.PostImageMapper$PostImageInsertCommand"
            useGeneratedKeys="true"
            keyProperty="postImageId"
            keyColumn="post_image_id">
        INSERT INTO post_image (
            post_id,
            object_key,
            sort_order
        ) VALUES (
            #{postId,jdbcType=NUMERIC},
            #{objectKey,jdbcType=VARCHAR},
            #{sortOrder,jdbcType=NUMERIC}
        )
    </insert>

    <delete id="deleteByPostId">
        DELETE FROM post_image
         WHERE post_id = #{postId,jdbcType=NUMERIC}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM post_image
         WHERE post_id = #{postId,jdbcType=NUMERIC}
           AND post_image_id IN
        <foreach collection="imageIds" item="imageId" open="(" separator="," close=")">
               #{imageId,jdbcType=NUMERIC}
        </foreach>
    </delete>

</mapper>
