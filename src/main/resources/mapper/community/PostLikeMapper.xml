<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ditto.community.repository.PostLikeMapper">

    <insert id="insertLike">
        INSERT INTO post_like (
            post_id,
            user_id,
            created_at
        ) VALUES (
            #{postId,jdbcType=NUMERIC},
            #{userId,jdbcType=NUMERIC},
            SYSTIMESTAMP
        )
    </insert>

    <delete id="deleteLike">
        DELETE FROM post_like
         WHERE post_id = #{postId,jdbcType=NUMERIC}
           AND user_id = #{userId,jdbcType=NUMERIC}
    </delete>

    <select id="existsByPostIdAndUserId" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
          FROM post_like
         WHERE post_id = #{postId,jdbcType=NUMERIC}
           AND user_id = #{userId,jdbcType=NUMERIC}
    </select>

    <select id="countByPostId" resultType="int">
        SELECT COUNT(*)
          FROM post_like
         WHERE post_id = #{postId,jdbcType=NUMERIC}
    </select>

</mapper>
