<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ditto.user.repository.UserMapper">

    <select id="countByEmail" parameterType="string" resultType="int">
        SELECT COUNT(*)
          FROM app_user
         WHERE email = #{_parameter}
    </select>

    <select id="findActiveByEmail" parameterType="string" resultType="com.ditto.user.repository.UserRow">
        SELECT user_id,
                name,
                email,
                password_hash,
                preferred_language_code,
                status,
                role,
                persona
          FROM app_user
         WHERE email = #{_parameter}
           AND status = 'ACTIVE'
           AND deleted_at IS NULL
    </select>

    <select id="findActiveById" parameterType="long" resultType="com.ditto.user.repository.UserRow">
        SELECT user_id,
                name,
                email,
                password_hash,
                preferred_language_code,
                status,
                role,
                persona
          FROM app_user
         WHERE user_id = #{_parameter}
           AND status = 'ACTIVE'
           AND deleted_at IS NULL
    </select>

    <insert id="insert" parameterType="com.ditto.user.repository.SignupUserCommand">
        INSERT INTO app_user (
            country_id,
            name,
            email,
            password_hash,
            preferred_language_code,
            status,
            role,
            persona,
            created_at
        ) VALUES (
            #{countryId},
            #{name},
            #{email},
            #{passwordHash},
            #{preferredLanguageCode},
            #{status},
            #{role},
            #{persona,jdbcType=VARCHAR},
            SYSTIMESTAMP
        )
    </insert>

    <update id="updatePersona">
        UPDATE app_user
           SET persona = #{persona,jdbcType=VARCHAR}
         WHERE user_id = #{userId,jdbcType=NUMERIC}
           AND status = 'ACTIVE'
           AND deleted_at IS NULL
    </update>

</mapper>
